[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[build]
  command = "npm run build"
  publish = "dist"

[dev]
  targetPort = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200