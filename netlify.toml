[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  # No need for included_files because pdfkit uses built-in fonts

[build]
  command = "npm run build"
  publish = "dist"

[dev]
  targetPort = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200